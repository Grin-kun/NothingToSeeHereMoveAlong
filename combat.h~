/*
 lmaodoitl8r
*/

#include "entity.h"

typedef struct wilderness
{
  int * x; // DANGEROUS X'ES FOR THIS WILDERNESS
  int * y;
} WILDERNESS;

void dowildbattle (PLAYER, WILDERNESS*);

void addrecttowild(WILDERNESS*, int topx, int topy, int bottomx, int bottomy);

void addlinetowild(WILDERNESS *, 
		   int ishorizontal, 
		   int goeast, 
		   int startx, 
		   int starty, 
		   int dist);

void addsquaretowild(WILDERNESS*, int x, int y);

